#include <stdio.h>

int main()
{ int i,n,temp,sum=0,digit;
scanf("%d ",&n);
temp=n;
for(i=n;i>0;i=i/10){
digit=i%10;
sum=sum+(digit*digit*digit);
}
if(sum==temp)
{
    printf("yes");
}
else{

        printf("no");
}
return 0;
}